# 树莓派后台 UI 风格复刻指南

这份文档的目标，是让你在树莓派项目里也能稳定复刻当前这个管理员后台的 UI 语言。

它不是泛泛而谈的“做一个好看的后台”，而是尽量把这套界面的设计方向、视觉变量、布局骨架、组件规则、实现顺序、以及给 AI 的提示方式都写清楚。  
你可以把这份文档直接交给树莓派那边的 AI，让它照着实现。

---

## 1. 这套 UI 到底是什么风格

一句话总结：

**温暖、克制、偏设备控制台气质的现代后台界面。**

如果拆成几个关键词，它是：

- 温暖中性色底
- 轻拟物玻璃感面板
- 设备控制台风格的信息密度
- 卡片化仪表盘布局
- 带一点工程感，但不冷冰冰
- 强调“设备状态可读性”，不是营销官网风

它不是下面这些风格：

- 不是纯黑客风终端界面
- 不是 SaaS 默认紫白后台
- 不是极简到没有层次的白板式管理页
- 不是炫技型 3D 或重动画页面

它更像：

- “设备运维后台 + 轻量数据看板”
- “本地控制台 + 现代卡片面板”

视觉感受上要让人觉得：

- 这是一个和硬件、设备、状态、考勤、识别相关的后台
- 页面有设计感，但不会抢信息的戏
- 看久了不累，信息层级很清楚

---

## 2. 我在这里用的技术方式是什么

这一版并没有用复杂前端框架，而是用了一套很稳的实现方式：

- 服务端模板渲染
- 单个全局 CSS 文件控制整体风格
- CSS 变量统一主题
- 通过公共布局模板复用侧边栏、顶部栏和消息条
- 通过可复用类名搭积木式组合页面

当前这套实现结构对应的是：

- 基础布局模板：[templates/base.html](/d:/code/ras_pi/templates/base.html)
- 首页仪表盘：[templates/dashboard.html](/d:/code/ras_pi/templates/dashboard.html)
- 成员页：[templates/members.html](/d:/code/ras_pi/templates/members.html)
- 用户详情页：[templates/user_detail.html](/d:/code/ras_pi/templates/user_detail.html)
- 人脸档案页：[templates/face_profiles.html](/d:/code/ras_pi/templates/face_profiles.html)
- 考勤页：[templates/attendance.html](/d:/code/ras_pi/templates/attendance.html)
- 全局样式：[static/styles.css](/d:/code/ras_pi/static/styles.css)

所以你让树莓派那边 AI 做的时候，最好也采用类似结构：

- 一个基础布局模板
- 多个页面模板继承这个基础模板
- 一个统一样式文件
- 所有颜色、圆角、阴影、状态色都走变量

这样做的原因很简单：

- 好统一风格
- 好维护
- 后面扩展页面不容易跑偏
- AI 也更容易持续产出一致的界面

---

## 3. 这套设计的核心气质

### 3.1 页面气质

页面应该给人的感受是：

- 柔和但专业
- 有控制台感，但不过分工业化
- 设备状态一眼能看懂
- 信息密度高，但不压迫

### 3.2 视觉策略

核心策略是：

- 背景不纯白，用暖灰米色渐变
- 卡片不纯色死板，用半透明浅色面板
- 边框很轻，只负责分层，不抢视觉中心
- 颜色主要靠青绿色作为主强调色
- 危险操作才用红色
- 状态提示用 pill / badge，不大面积染色

### 3.3 为什么这样做

因为树莓派这类系统不是内容产品，而是“设备和状态管理界面”。  
这种界面最重要的不是“惊艳”，而是：

- 读起来舒服
- 一眼知道当前是否正常
- 操作入口稳定
- 能长时间盯着看

所以这套风格避免了：

- 过亮纯白
- 过深纯黑
- 过多艳色
- 过重阴影
- 过多动画

---

## 4. 颜色系统是什么

这套 UI 的视觉基础，来自下面这组 CSS 变量：

```css
:root {
    --bg: #f4f0e8;
    --bg-strong: #e8dcc6;
    --panel: rgba(255, 251, 245, 0.92);
    --panel-strong: #fff8ee;
    --text: #1b2730;
    --muted: #5d6770;
    --line: rgba(27, 39, 48, 0.12);
    --accent: #0f766e;
    --accent-strong: #0a4e54;
    --danger: #b42318;
    --shadow: 0 20px 60px rgba(38, 52, 63, 0.12);
    --radius: 24px;
}
```

### 4.1 配色解释

- `--bg`
  - 页面整体背景的暖灰米色基底
- `--panel`
  - 卡片主体颜色，略带透明感
- `--text`
  - 正文和核心信息颜色，偏深蓝灰，不是纯黑
- `--muted`
  - 次级说明文字颜色
- `--line`
  - 轻边框颜色
- `--accent`
  - 主品牌强调色，偏青绿
- `--accent-strong`
  - 更深一点的强调色，用于按钮和 hover
- `--danger`
  - 删除、错误、危险反馈色

### 4.2 这组颜色想表达什么

- 米色底让页面更温和
- 青绿色让页面有“设备系统 / 状态稳定 / 技术控制”的感觉
- 深蓝灰文本比纯黑更高级，也更耐看
- 红色只用于真正风险动作，避免颜色失真

### 4.3 迁移到树莓派时不要做什么

不要把它改成：

- 纯白底 + 紫色按钮
- 深黑底 + 霓虹绿
- 大面积蓝色渐变 SaaS 风
- 红黄蓝全上导致设备信息失焦

---

## 5. 字体和排版是什么策略

### 5.1 当前选择

正文使用：

```css
font-family: "Segoe UI Variable", "Noto Sans SC", "Microsoft YaHei", sans-serif;
```

标题使用：

```css
font-family: "Bahnschrift", "Segoe UI Variable", sans-serif;
```

### 5.2 为什么这么搭

- 正文字体要稳定、清晰、适合中文
- 标题字体要比正文更有控制台和工程系统感
- `Bahnschrift` 有一点工业感，适合设备后台标题

### 5.3 排版原则

- 大标题不夸张，但要明显
- 说明文字尽量 1 到 2 行，说清楚页面用途
- 次级信息用 muted 色
- 小标签使用全大写英文 eyebrow 风格

例如：

- `LOCAL CONSOLE`
- `DEVICE STATUS`
- `ATTENDANCE`

这种做法的好处是：

- 页面更有系统感
- 导航和模块分区更明确
- 不需要重色块也能做层级

---

## 6. 布局骨架是什么

整套后台的核心骨架是：

- 左侧固定导航
- 右侧主内容区
- 顶部标题栏
- 页面顶部 Hero 区
- 下方按模块分布的卡片区

基础结构如下：

```html
<div class="shell">
  <aside class="sidebar">...</aside>
  <main class="main">
    <header class="topbar">...</header>
    <section class="hero">...</section>
    <section class="grid ...">
      <article class="card">...</article>
    </section>
  </main>
</div>
```

### 6.1 左侧导航

作用：

- 让后台始终像“控制台”而不是普通网页
- 给不同功能页稳定的切换路径

特征：

- 宽度固定
- 浅玻璃背景
- 当前页高亮
- hover 向右轻微位移

### 6.2 顶部栏

作用：

- 告诉用户当前在哪个页面
- 给页面补充一句系统定位

特征：

- 左侧是标题
- 右侧是简短说明
- 不做复杂工具栏

### 6.3 Hero 区

这是这套风格很关键的一层。

每个页面顶部不是直接开始表格，而是先有一个说明区：

- 页面主题
- 一句页面说明
- 1 到 2 个主要动作按钮
- 少量状态 badge

这样做的好处：

- 页面打开时更有“掌控感”
- 用户知道这页能干什么
- 比“上来就是表格”更完整

---

## 7. 背景和层次是怎么做的

这套背景不是单色，而是：

- 大底色渐变
- 两个固定的模糊氛围光斑
- 半透明卡片面板

核心思路：

```css
body {
    background:
        radial-gradient(circle at top left, rgba(15, 118, 110, 0.18), transparent 30%),
        radial-gradient(circle at bottom right, rgba(181, 101, 29, 0.16), transparent 30%),
        linear-gradient(160deg, #f8f5ef 0%, #f1e7d8 100%);
}
```

再叠加两个 `.ambient` 光斑：

- 右上青绿色
- 左下暖棕色

### 为什么不是纯色背景

因为纯色背景容易让后台看起来：

- 太平
- 太廉价
- 没有空间层次

而这种方式：

- 不会干扰信息
- 但会让页面有氛围
- 看起来更完整

---

## 8. 卡片系统怎么做

卡片是这套 UI 的主角。

基础卡片类：

```css
.card {
    padding: 24px;
    background: var(--panel);
    border: 1px solid var(--line);
    border-radius: var(--radius);
    box-shadow: var(--shadow);
}
```

### 8.1 普通卡片

用于：

- 设备健康
- 性能指标
- 表格容器
- 用户详情

### 8.2 统计卡片

用于：

- 今日签到人数
- CPU 温度
- 缺勤人数

特点：

- 数字大
- 标签小
- 说明简短

### 8.3 聚光卡片 / Spotlight

这是首页最有风格的一块。

比如“实时识别状态”：

- 不只是列字段
- 而是重点放大最值得看的状态
- 下面再补细节信息

建议树莓派端 AI 学这一点：

**不要把重要状态全部做成普通表格字段。**

应该把最关键的一块做成：

- 重点卡片
- 更强背景层次
- 大数字 + 状态标签 + 最近行为

这类信息在你的场景里通常包括：

- 摄像头是否运行
- 最近一次识别结果
- 最近一次签到写入
- 当前检测到的人脸数量

---

## 9. 组件语言有哪些

### 9.1 Pill / Badge

用于表达轻量状态：

- 在线 / 异常
- active / disabled
- ready / 未就绪
- 采集中 / 空闲中

特点：

- 圆角胶囊
- 小尺寸
- 低饱和背景
- 不做实心大色块压屏

### 9.2 Hero Buttons

主要按钮：

- 青绿色渐变
- 圆角较大
- hover 轻微上浮

次要按钮：

- 浅色背景
- 边界清楚
- 不和主按钮抢权重

### 9.3 Filters

筛选区不是零散控件，而是一块整齐的表单区：

- 日期
- 编号
- 姓名
- 限制条数

统一放进 `filters` 网格里。

### 9.4 Table

表格样式的目标不是“炫”，而是“清楚”：

- 表头大写或小标题感
- 行高舒适
- 边线轻
- 文本左对齐

### 9.5 Meter Bar

用于性能页：

- 内存使用率
- 磁盘使用率

这种“条形进度 + 百分比”的呈现，比纯文本更直观。

---

## 10. 这套 UI 的信息组织方法

不是简单“把接口字段全展示”，而是做了信息分层。

### 第一层：一眼看懂

通过顶部统计卡和状态 badge 表达：

- 设备是否正常
- 今日签到多少
- 识别线程是否运行
- 是否存在风险

### 第二层：关键模块

通过重点卡片表达：

- 实时识别状态
- 最近一次成功写入
- 资源占用情况

### 第三层：详细数据

通过 `detail-grid` 或表格表达：

- hostname
- fqdn
- 路径
- 时间
- 状态字段

这就是为什么页面会显得整洁：

- 不是所有信息都同等权重
- 最重要的信息先被视觉放大

---

## 11. 树莓派那边应该怎么复刻

如果你要让树莓派项目本身也用这套风格，推荐它照这个顺序做。

### 第一步：先抽一个基础布局

让 AI 先做：

- `base.html`
- `styles.css`
- `sidebar`
- `topbar`
- `hero`
- `card`
- `grid`

先不要一上来就做所有业务页。

### 第二步：先做首页

优先做一个有代表性的首页：

- 顶部状态
- 摄像头状态
- 设备健康
- 性能指标
- 当日汇总

因为首页最能验证整套风格有没有跑偏。

### 第三步：再做列表页

列表页包括：

- 成员页
- 用户页
- 人脸档案页
- 考勤页

这些页只要继承首页已经确定的视觉语言，就能保持统一。

### 第四步：最后做详情页和操作页

例如：

- 用户详情
- 配置页
- 日志页
- 系统设置页

---

## 12. 给树莓派 AI 的明确实现要求

你可以直接把下面这段要求给它。

### 12.1 简版指令

请为树莓派后台实现一套与当前管理员系统一致的 UI 风格，要求如下：

- 风格是温暖中性色底的现代设备控制台后台，不要做成默认 SaaS 紫白风
- 使用卡片化布局、左侧导航、顶部标题栏、页面 Hero 区
- 使用暖米色背景渐变、浅玻璃感面板、青绿色作为主强调色、红色只用于危险操作
- 统一使用 CSS 变量控制主题
- 首页要突出“设备状态、摄像头状态、最近识别、最近签到、性能指标”
- 重要状态使用 pill/badge，不要大面积高饱和色块
- 表格页保持干净、清楚、轻边框、高可读性
- 桌面端和移动端都要能正常显示
- 不要使用通用模板味很重的管理后台样式

### 12.2 更强约束版指令

请严格参考以下设计语言实现树莓派后台页面：

- 页面底色为暖灰米色渐变，不是纯白
- 使用半透明浅色面板卡片，圆角较大，阴影柔和
- 主强调色为青绿色，按钮使用青绿色渐变
- 标题字体应有一点工程/控制台气质，正文字体优先保证中文清晰
- 布局必须包含：左侧导航、顶部栏、Hero、卡片网格、表格容器
- 首页必须包含一个“重点状态卡”，用于放大显示摄像头运行、最近识别结果、最近考勤写入
- 性能指标请使用进度条或 meter bar，而不是只显示数字
- 状态字段请使用 pill/badge 展示，例如 online、running、active、disabled、ready
- 不要做成营销官网，不要做成紫白 SaaS，不要做成纯黑终端，不要过度动画

---

## 13. 最好直接复用的类名体系

树莓派那边如果也是 HTML + CSS，最省心的方法不是“重新想命名”，而是尽量复用我这里的类名体系：

- `shell`
- `sidebar`
- `brand`
- `nav`
- `nav-link`
- `main`
- `topbar`
- `topbar-note`
- `hero`
- `hero-copy`
- `hero-actions`
- `hero-badges`
- `grid`
- `grid-2`
- `grid-3`
- `grid-4`
- `card`
- `stat-card`
- `section-heading`
- `filters`
- `stacked-form`
- `detail-grid`
- `detail-grid compact`
- `pill`
- `pill-ok`
- `pill-warn`
- `pill-danger`
- `pill-soft`
- `table-badge`
- `table-badge-ok`
- `table-badge-muted`
- `status-dot`
- `status-live`
- `status-idle`
- `spotlight-card`
- `spotlight-metrics`
- `metric-tile`
- `record-callout`
- `meter-stack`
- `meter-card`
- `meter-row`
- `meter-bar`
- `profile-grid`
- `profile-card`

这样做的好处：

- 风格迁移速度快
- AI 不容易发明新结构导致跑偏
- 后面两边风格能保持一致

---

## 14. 页面级映射建议

树莓派后台如果也要做成这套风格，页面映射建议如下。

### 首页 Dashboard

包含：

- 设备在线状态
- 摄像头线程状态
- 最近一条识别消息
- 最近一条签到写入
- CPU 温度
- 内存 / 磁盘使用
- 当日考勤汇总

### 成员 / 用户页

包含：

- 成员名单表格
- 用户表格
- 注册状态
- 人脸是否就绪
- 用户详情跳转

### 人脸档案页

包含：

- 图片卡片
- 用户 ID
- 编号
- 创建时间
- 删除入口

### 考勤页

包含：

- 统计卡
- 过滤器
- 考勤表格
- 快照预览或查看入口

### 配置 / 系统页

如果树莓派那边还有系统配置页，建议也沿用这套风格：

- 配置项分组卡片
- 危险操作独立卡片
- 状态 badge
- 明确的保存按钮和提示消息

---

## 15. 树莓派 AI 做的时候最容易犯的错误

### 错误 1：做成默认管理后台模板

表现：

- 全白底
- 蓝色按钮
- 细小字体
- 没有 Hero 区
- 没有气氛层次

这是最常见跑偏。

### 错误 2：做成酷炫大屏风

表现：

- 过深色背景
- 高对比荧光色
- 大量动画
- 图表重于信息

树莓派后台不是大屏展示页，不要走这个方向。

### 错误 3：字段全平铺

表现：

- 首页像 JSON 展开页
- 所有字段都用同样权重展示

应该把最关键的状态做视觉放大。

### 错误 4：每一页重新发明设计

表现：

- 详情页和列表页像不同系统
- 按钮颜色不统一
- 圆角大小不统一

所以必须先建立公共样式系统。

---

## 16. 推荐的实现步骤

建议树莓派 AI 按下面顺序实现：

1. 先写全局 CSS 变量、背景、按钮、卡片、表格、badge。
2. 再写基础布局 `base.html`。
3. 再完成首页 `dashboard.html`。
4. 确认首页风格正确后，再复制出列表页模板。
5. 最后补详情页、配置页、日志页。

不要反过来。

因为一旦首页风格确定，剩下页面都只是扩展，不容易崩。

---

## 17. 可以直接交给树莓派 AI 的最终说明

下面这一段你可以几乎原样发给它。

---

请把树莓派后台的 UI 做成和当前 Windows 管理端一致的设计语言。

目标风格：

- 温暖中性色底的现代设备控制台后台
- 不要做成默认紫白 SaaS 风
- 不要做成纯黑极客终端风
- 要有设备管理、状态监控、控制台的气质

视觉要求：

- 页面背景使用暖灰米色渐变
- 使用两个轻微的模糊氛围光斑增强层次
- 卡片使用浅色半透明玻璃感面板
- 主色使用青绿色，危险操作使用红色
- 标题更有工程系统感，正文保证中文清晰
- 圆角偏大，阴影柔和，边框轻

布局要求：

- 左侧导航
- 顶部标题栏
- 每页顶部 Hero 区
- 下方卡片化网格布局
- 列表页使用干净的表格容器

组件要求：

- 用 pill / badge 展示状态
- 首页必须有重点状态卡，用于突出摄像头运行、最近识别、最近签到
- 性能指标使用进度条 / meter bar
- 表单筛选区保持整齐的网格布局

实现要求：

- 先抽离公共布局和全局样式
- 使用 CSS 变量统一主题
- 尽量复用以下类名体系：shell、sidebar、topbar、hero、card、grid、pill、table-badge、spotlight-card、meter-bar
- 先完成 dashboard，再扩展到其他页面

请输出完整可运行的模板和样式，不要只给设计描述。

---

## 18. 最后一句最重要的原则

如果树莓派那边的 AI 只能记住一句话，那就让它记住这个：

**这套 UI 不是为了“做一个漂亮网页”，而是为了“做一个温暖、清晰、可长期使用的设备控制台”。**

只要它围绕这个目标去做，最终结果就不会偏得太离谱。
